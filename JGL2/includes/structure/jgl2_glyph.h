#pragma once

#include "jgl2_basic_types.h"
#include "jgl2_exception.h"

namespace jgl
{
	/*
	class Glyph
	{
	private:
		jgl::Size_t _size = 0;
		jgl::Char _content[5];

		jgl::Int _countCharLen(const jgl::Char p_toParse);
		void _decodeUnicodeChar(const jgl::Char* p_str);

	public:
		Glyph();
		Glyph(jgl::WChar p_c);

		jgl::Size_t size() const;

		const char& operator [] (jgl::Size_t p_index) const;
		void clean();
	};
	*/
}
